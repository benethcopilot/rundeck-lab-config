project.name=Lab
project.description=Rundeck lab project — target and runner under test

# Default node executor: SSH.
service.NodeExecutor.default.provider=jsch-ssh
service.FileCopier.default.provider=jsch-scp

# Use the lab SSH key from Rundeck key storage on every node.
project.ssh-authentication=privateKey
project.ssh-key-storage-path=keys/lab/id_rsa
project.ssh-keypath=
# The `lab` user is baked into both target and runner images.
project.ssh-user=lab
# Lab only: skip strict host key checking. Rundeck picks this up via JSCH.
project.jsch-ssh.ssh-strict-host-key-checking=no

# File-based resource source reading resources.xml placed by sync-config.sh.
resources.source.1.type=file
resources.source.1.config.file=/var/rundeck/projects/Lab/etc/resources.xml
resources.source.1.config.format=resourcexml
resources.source.1.config.generateFileAutomatically=false
resources.source.1.config.includeServerNode=true
resources.source.1.config.requireFileExists=true
resources.source.1.config.writeable=false
